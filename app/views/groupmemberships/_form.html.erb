<%= form_with model: @membership do |f| %>

  <%= f.collection_select :group_id, Group.all, :id, :name %>
  <%= f.label :plane, "Plane" %>
  <%= f.select :plane, options_for_select(pre_define_planes) %>

  <%= f.datetime_field :membershipdate %>

  <%= f.submit %>
  
<% end %>
